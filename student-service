pipeline {
    agent{
        label "MS"
    }

    stages {
        stage('git-checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/Puspanjali333/student-service.git'
            }
        }
    }
}
